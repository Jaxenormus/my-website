---
interface Props {
	title: string;
	description: string;
}

const { title, description } = Astro.props;
---

<article class="space-y-1">
  <h3 class="text-sm lg:text-base">{title}</h3>
  <p class="text-zinc-400 text-sm lg:text-base">{description}</p>
</article>
